const config = {
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  trailingComma: "all",
  printWidth: 100,
  endOfLine: "auto",
  plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
